name: Prettier

on:
  push:
    branches:
      - main

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: Format with Prettier
        run: npm run format

      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git commit -m "Auto-format with Prettier"
          git push https://${{ secrets.GITHUB_GH_TOKEN }}@github.com/ic045-sistemaacademico-2023/frontend-sistema-academico-ic-ufba.git HEAD:main
